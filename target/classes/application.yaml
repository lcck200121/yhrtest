server:
  port=8080:

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/reportday?useUnicode=true&characterEncoding=UTF-8&serverTimezone=UTC
    username: root
    password: 123456
    driver-class-name: com.mysql.cj.jdbc.Driver

  redis:
    host: localhost
    port: 6379

#整合mybatis
mybatis:
  type-aliases-package: com.yhr.pojo
  mapper-locations: classpath:myBatis/Mapper/*.xml

  spring:
    jackson:
      time-zone: GMT+8
      date-format: yyyy-MM-dd






